<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting.
         Uses Theme.Light.NoTitleBar so window background is white.
         windowBackground points to launch_background which now shows
         the same logo as the Flutter SplashScreen — seamless visual transition. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Pre-Android 12: shows white background + centered ic_launcher logo -->
        <item name="android:windowBackground">@drawable/launch_background</item>

        <!-- Android 12+ System Splash Screen.
             windowSplashScreenAnimatedIcon uses @mipmap/ic_launcher (generated
             from dp_logo2.png — same image as Flutter's Images.logo).
             This makes Android 12+ system splash match Flutter splash exactly. -->
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/white</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>
    </style>

    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>